# Personal-Frameworks

Personal automation frameworks for Claude Code.

## claude-context-automation

A merged context automation framework for Claude Code combining:
- Practical automation layer: session state, JSONL tracking, skills, prompt shortcuts
- Governance layer: 11-level precedence hierarchy, memory taxonomy, validation, approval gates

### Structure
- `global/` → install to `~/.claude/`
- `project-level/` → install to your project root + `.claude/`

76/76 validation checks passing.
